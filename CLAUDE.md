# Skills工作流 · 收录标准与写法约束（**唯一权威**）

> **本文件是本库的约束来源。** 任何一篇的内容与命名，若与本文件冲突，**以本文件为准**。
> 本库**会被整体移动 / 改名** ⇒ 库内一切内容**不得依赖库外文件才能读通**。
> 建立：2026-09-16 · 用户指令：「入库要求要求和 `agent-pitfalls-kb` 一样高，是给 agent 看的」

---

## 0 · 与 `agent-pitfalls-kb` 的关系（**先说清，免得读的人以为这是同一套**）

**入库要求的「高度」与判定「是否达到」的「定义」，两边一致；但「判什么内容」与「方向」不同。**

| | `agent-pitfalls-kb` | **本库** |
|---|---|---|
| **方向** | **负向** —— 记**犯过的错**（避坑） | **正向** —— 记**怎么用**（用法 / 用处 / 注意点） |
| **尺子一** | **严重性**：不报错、不变红，却能让错的结论被交付 | **可复用性** |
| **尺子二** | **领域**：必须是 Agent/harness/AI 系统本身的问题 | **有依据** |
| **判定问法** | 「把 Agent 换成**一个普通工程师**，这个坑还成立吗？」 | 见 §2 |

**照抄它的**：读者定义（§1）· **两把尺子的严格度**（必须**同时**满足）· 必备体例（§7）·
编号（§6）· 路径约束（§8）· 自检清单（§10）· 「删一条必须写理由」（§9）

**自己定的**：两把尺子的**内容**（§2）· **方向**（正向）

---

## 1 · 读者是谁（这条决定一切）

**唯一读者 = AI Agent。** 人是维护者、不是读者。

⇒ 由此推出三条硬要求：

1. **写规则，不写感想。** 每条必须是"Agent 读了能照做"的动作；不写情绪、不写"我们要重视"。
2. **不许要求读者去库外翻东西。** 库外的东西对读者**不存在**（他拿不到、也不该去拿）。
3. **能被扫读。** Agent 是按需检索 + 读局部，不是从头读到尾 ⇒ 标题要能直接命中主题，规则要能独立成立。

---

## 2 · 收什么（**两道关，必须都过**）

### 关一 · 两把尺子（够不够格）—— **必须同时满足**

#### 尺子一 · 可复用性

**判定问法：把这条用法挪到「另一个项目」上，还成立吗？**

- ✅ **成立** ⇒ 过
- ❌ **只对某一个项目成立** ⇒ **那是项目文档，不收**
  （与 `agent-pitfalls-kb` §3「单个项目的流程规约、目录约定、团队分工 —— 不收」**同一条道理**）

#### 尺子二 · 有依据

**判定问法：这条结论，跑一遍能复现吗？**

- ✅ **给得出复现步骤 / 有实跑记录** ⇒ 过
- ❌ **给不出** ⇒ **不写进正料** —— 降为**线索**（标 C 类），**只作引用**

> ⚠️ **为什么门槛要这么高**（用户 2026-09-16 原话）：
> 「**来源要按住，以后才不会越走越偏。门槛必须要高** ——
> **量少没事，要精，且经得住事实考验和论证。**」

### 关二 · 来源分类 A / B / C（是不是你的）

**过了尺子还不够** —— 还要标清**这份内容是谁的**。**两道关都过，且「类」列不能空。**

| 类 | 是什么 | 能对外用吗 |
|---|---|---|
| **A · 自己做 + 实测过** | 你的整合、改造、测试、实测报告 | ✅ |
| **B · 第三方开源** | 别人写的 skill 库与介绍 | ❌ |
| **C · 二手整理稿** | 从别人的文章 / 站点整理的清单 | ❌ **只作线索** |

**「类」怎么判 —— ⛔ 看「判断」归谁，不看「字」是谁敲的：**

| # | 问 | 是 → | 否 → |
|---|---|---|---|
| **①** | 里面的**取舍 / 推荐 / 排序**，是你下的吗？ | **A** | **C** |
| **②** | **删掉你**，别人能不能整理出同样的东西？ | **C** | **A** |
| **③** | 一句话：**搬运事实**，还是**产出判断**？ | 搬运 → **C** | 判断 → **A** |

⛔ **不要拿「谁敲的字」当判据**：**AI 代笔、但取舍是你下的 → 仍然是 A。**
反过来：**亲手抄的、但判断是别人的 → 还是 C。**

> 📌 **混合最常见**（一份东西里既有搬运的事实、又有你的取舍）
> ⇒ 标 **「A / B / C 混合」**，**并在说明里点出「哪一部分是你的」**。

---

## 3 · 不收什么（明确列出，防「越走越宽」）

| 不收 | 为什么 / 去哪 |
|---|---|
| **单个项目的流程规约、目录约定、团队分工** | 那是**项目文档**，不是可复用的用法 |
| **第三方 skill 的介绍 / 榜单 / 评测**（非本人产出） | **不进正料**；要留 ⇒ 标 **B / C**，只作线索 |
| **只对某一次、某一台机器成立的东西** | **不可复用**（尺子一不过） |
| **纯管理 / 协作话题**（排期、评审文化、汇报） | 与本库用途无关 |
| **skill 本体**（`SKILL.md` + `harness/`） | 那不是"用法"，是"成品" |
| **本机机器级设置方案**（跨项目、装到新机器那种） | **作用域不对** —— 那不是项目级 / 用法级 |
| **只对「人」成立的技巧** | 读者是 Agent；若同时对人有用，**按"对 Agent 有用"写** |

---

## 4 · 目录与分区

```
Skills工作流-skills-workflow/
├── CLAUDE.md                      ← 本文件（约束；不参与编号）
├── README.md                      ← 人类入口 + 内容索引
└── NN-中文名/                      ← 一个主题 = 一个独立子文件夹
```

**分区规则**：

- **一个主题 = 一个子文件夹**，用 `NN-中文名`（两位递增）。
- **不收的域不建区**；某区**长期为空 ⇒ 删掉**（不保留空壳）。
- 新增区前先问「**能不能并进现有区**」；并得进就不新建。

---

## 5 · 命名

| 对象 | 格式 | 例 |
|---|---|---|
| 库根目录 | `中文 + 英文仓库名`（两者互为翻译） | `Skills工作流-skills-workflow` |
| 子文件夹 | `NN-中文名` | `00-生态导航` · `01-双框架工作流` |
| 文档 | `英文功能词中文释义-限定词.md` | `skills生态导航-2026-09.md` |
| 保留字 | 不套格式 | `README.md` · `CLAUDE.md` · `.gitignore` |

⛔ **本库内不许混用多套规范。**

---

## 6 · 编号

**格式：`<区号>-R<区内序号>`**，如 `01-R3`。

- ✅ **全局唯一**（彻底避免"同一编号指两条规则"）
- ✅ 区内连续 ⇒ 增删条目不牵动别区
- ✅ **库被移动 / 改名不影响规则 ID**（只影响文件名）
- ⚠️ **规则搬家 ⇒ ID 会变**：搬家时**必须**在该区「变更记录」里写 **`旧 ID → 新 ID`**
- ❌ 全库连续编号**禁止** —— 它必然产生跨区冲突

**跨区引用**：**只写区号或规则 ID**（如「见 `01-R3`」），**不写文件名、不写"第 N 个文件夹"**。

---

## 7 · 每篇体例（**必备五节，缺一节即不合规**）

1. **一句话结论**（最值得留下的那句，放最前）
2. **用法**（每条：`用法(通用)` → `反例(怎么用会坏)` → `正例(怎么用对)`）
3. **适用场景与不适用场景**（读者要能判断"这条对当前任务适不适用"）
4. **来源**（事件描述，见 §8；并标 **A / B / C**）
5. **怎么验证它被落地**（可证伪：**违反它时，有没有东西会变红 / 出声？**）

> 没有第 5 节的策略 = "只有文字"，**等于没落地** —— 这是本库的立库之基。

---

## 8 · 路径约束（硬性）

> **背景**：本库会被移动；单个项目做完 / 改名 / 重构后，写进库的路径**必然失效**。

| 允许 | 规则 |
|---|---|
| ❌ **项目产物路径**（`docs/复盘/xxx.md`、`<某项目>/tools/yyy.py`） | **禁止**。来源只写**事件描述**：`<项目代号>` + 日期 + **哪一次实战** |
| 🟡 **约定位置**（`.claude/hooks/`、`.claude/rules/`、`CLAUDE.md`、`tests/`、`tools/`） | 可写，但**必须声明这是跨项目的约定位置**，不是某个项目的产物 |
| 🟡 **具体脚本 / 文件名** | 可写，但**必须加前缀**「**示例（本库实现）**」—— 否则会被读成"标准要求" |
| 🟢 **叙事里的示意文件名** | 可写，**无需逐个标注** —— 它是**举例**，不是路径 |
| ❌ **`第 N 个文件夹`式引用** | 禁止（见 §6）：改用区号 |

**自检句（可证伪）**：把本库**整体挪到一个全新位置**，删掉一切库外文件 ——
**这篇还读得懂、还照做得了吗？** 读不懂 ⇒ 不合规，回去改。

---

## 9 · 维护流程

| 动作 | 规则 |
|---|---|
| **新增一个区** | 先判 §2 **两道关**；不过 ⇒ 不收。新文件夹用区内最大号 +1 |
| **新增一条用法** | 追加到**已有区**的末尾（不新建区），编号取该区下一个序号 |
| **规则搬家** | 允许；**必须**在迁出 / 迁入两区的变更记录里写 `旧 ID → 新 ID` |
| **删除** | 仅当**被证伪**（实证不成立）或**并入他区**；删除**必须**写理由 —— **「删一条用法 = 丢掉一条用法」** |
| **整库移动 / 改名** | 允许且预期；**移动后必须做** §8 的自检句 |
| **改库前先自查** | 过一遍 §10 自检清单 |
| **本库已纳入 git** | 每次改动 = **一个 commit**；**规则搬家必须**在提交信息里带 `旧 ID → 新 ID` |

---

## 10 · 自检清单（发布一篇前过一遍）

- [ ] **读者是 Agent？** 有没有写成"给人看的感想 / 管理建议"？
- [ ] **尺子一过？** 换个项目**还成立**吗？（还是只对这个项目成立？）
- [ ] **尺子二过？** **跑一遍能复现**吗？复现步骤 / 实跑记录在哪？
- [ ] **标了类？** A / B / C（混合的要写清**哪一部分是你的**）
- [ ] **必备五节齐**（§7）？**不适用场景**写了吗？
- [ ] **编号**是 `<区号>-R<n>`（§6）？跨区引用**只写 ID**？
- [ ] **零项目产物路径**（§8）？实现示例都带「示例（本库实现）」前缀？
- [ ] **第 5 节可证伪**：**违反它时，有没有东西会变红 / 出声？**

---

## 变更记录

- **2026-09-16 建立。** 起因：用户指出本库**缺一条写下来的收录标准** ——
  且要求**入库要求与 `agent-pitfalls-kb` 一样高**（原话：「**来源要按住，以后才不会越走越偏。
  门槛必须要高 —— 量少没事，要精，且经得住事实考验和论证。**」）。
  同轮确定：**两把尺子的内容**（可复用性 / 有依据）与 pitfalls **不同**（严重性 / 领域）；
  **方向不同**（本库正向记用法，pitfalls 负向记坑）；**与「来源分类 A/B/C」并存**（两道关）。
  形式（读者定义 / 两条尺子的严格度 / 必备体例 / 编号 / 路径约束 / 自检 / 可证伪）**照 pitfalls 抄** ——
  它是本项目里**唯一一条已被证明有效**的库标准。
